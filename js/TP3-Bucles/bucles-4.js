// 4- Realiza un script que pida números hasta que se pulse “cancelar”. Si no es un número deberá indicarse con un «alert» y 
// seguir pidiendo números. Al salir con “cancelar” deberá indicarse la suma total de los números introducidos.

// let suma = 0;

// while (true) {
//   let entrada = prompt("Ingrese un numero o puedes presionar 'Cancelar' para detenerse");

//   if (entrada === null) {
//     break;
//   }

//   let numero = parseInt(entrada);

//   if (!isNaN(numero)) {
//     suma += numero;
//   } else {
//     alert("No es un numero válido. Intente nuevamente.");
//   }
// }

// if (suma !== 0) {
//   alert("La suma total de los numeros introducidos es: " + suma);
// } else {
//   alert("No se introdujeron numeros.");
// }